# XAgent

XAgent is a Python coding assistant CLI.

## Quick start

1. Create a virtual environment and install the project.
2. Run `xagent config init`.
3. Update the generated project-local config in `.xagent/config.toml`.
4. Export the API key environment variable referenced by your config.
5. Run `xagent run "Say hello"`.
